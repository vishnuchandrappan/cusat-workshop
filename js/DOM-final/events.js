const alertClick = () => {
  alert("onClick Event");
};

const alertOnChange = (event) => {
  alert(e.target.value);
};

const alertMouseOver = () => {
  alert("mouse over");
};

const alertMouseOut = () => {
  alert("mouse out");
};
